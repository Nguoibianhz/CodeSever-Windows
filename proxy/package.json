{
  "name": "windows-code-server-jwt-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "JWT reverse proxy for code-server on Windows",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "http-proxy": "^1.18.1",
    "jsonwebtoken": "^9.0.2"
  }
}
